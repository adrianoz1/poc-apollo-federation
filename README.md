# (POC) Microsservices + Apollo Federation

### install
- comments-services, geteway, posts-services
```npm i```

### generate composite (supergraph)
```rover supergraph compose --config ./supergraph.yaml > prod-schema.graphql```
